# Production build config — used automatically by `vite build`, NOT by `npm run dev`.
# This is a public URL (safe to commit), so the deployed site loads media from R2
# without any host dashboard setup. Change it if you later move to a custom domain.
VITE_MEDIA_BASE=https://pub-28fdf413b1dd44c2996c09439f65b876.r2.dev
