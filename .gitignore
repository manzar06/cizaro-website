# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
!.env.production

# Heavy video/media — served from Cloudflare R2 in production, kept locally for
# dev only. Excluding it keeps the repo + app deploy tiny (see DEPLOY.md).
/public/media
/media-r2
# stray full-length source downloads left in the project root (safe to delete;
# highlights were already extracted). Never commit these — they blow the push.
/*_1080p.mp4

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
